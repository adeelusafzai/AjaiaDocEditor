# Base URL of the API server (no trailing slash).
# Local dev default. In production set this to your deployed API origin,
# e.g. https://ajaia-docs-api.onrender.com
VITE_API_URL=http://localhost:4000
